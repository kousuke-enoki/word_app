# ビルド用のステージ
FROM golang:1.23 AS builder

# 作業ディレクトリを設定
WORKDIR /app

# Goのモジュールをコピー
COPY go.mod go.sum ./
RUN go mod download

# アプリケーションのソースコードをコピー
COPY . .

# airをインストール (修正)
RUN go install github.com/air-verse/air@latest

# アプリケーションをビルドしないので、CMDを変更
CMD ["air"]