// Code generated by ent, DO NOT EDIT.

package ent

import (
	"encoding/json"
	"fmt"
	"strings"
	"time"
	"word_app/backend/ent/exam"
	"word_app/backend/ent/user"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
)

// Exam is the model entity for the Exam schema.
type Exam struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// UserID holds the value of the "user_id" field.
	UserID int `json:"user_id,omitempty"`
	// TotalQuestions holds the value of the "total_questions" field.
	TotalQuestions int `json:"total_questions,omitempty"`
	// CorrectCount holds the value of the "correct_count" field.
	CorrectCount int `json:"correct_count,omitempty"`
	// IsRunning holds the value of the "is_running" field.
	IsRunning bool `json:"is_running,omitempty"`
	// TargetWordTypes holds the value of the "target_word_types" field.
	TargetWordTypes string `json:"target_word_types,omitempty"`
	// ChoicesPosIds holds the value of the "choices_pos_ids" field.
	ChoicesPosIds []int `json:"choices_pos_ids,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the ExamQuery when eager-loading is set.
	Edges        ExamEdges `json:"edges"`
	selectValues sql.SelectValues
}

// ExamEdges holds the relations/edges for other nodes in the graph.
type ExamEdges struct {
	// User holds the value of the user edge.
	User *User `json:"user,omitempty"`
	// ExamQuestions holds the value of the exam_questions edge.
	ExamQuestions []*ExamQuestion `json:"exam_questions,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [2]bool
}

// UserOrErr returns the User value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e ExamEdges) UserOrErr() (*User, error) {
	if e.User != nil {
		return e.User, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: user.Label}
	}
	return nil, &NotLoadedError{edge: "user"}
}

// ExamQuestionsOrErr returns the ExamQuestions value or an error if the edge
// was not loaded in eager-loading.
func (e ExamEdges) ExamQuestionsOrErr() ([]*ExamQuestion, error) {
	if e.loadedTypes[1] {
		return e.ExamQuestions, nil
	}
	return nil, &NotLoadedError{edge: "exam_questions"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*Exam) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case exam.FieldChoicesPosIds:
			values[i] = new([]byte)
		case exam.FieldIsRunning:
			values[i] = new(sql.NullBool)
		case exam.FieldID, exam.FieldUserID, exam.FieldTotalQuestions, exam.FieldCorrectCount:
			values[i] = new(sql.NullInt64)
		case exam.FieldTargetWordTypes:
			values[i] = new(sql.NullString)
		case exam.FieldCreatedAt:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the Exam fields.
func (e *Exam) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case exam.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			e.ID = int(value.Int64)
		case exam.FieldUserID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field user_id", values[i])
			} else if value.Valid {
				e.UserID = int(value.Int64)
			}
		case exam.FieldTotalQuestions:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field total_questions", values[i])
			} else if value.Valid {
				e.TotalQuestions = int(value.Int64)
			}
		case exam.FieldCorrectCount:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field correct_count", values[i])
			} else if value.Valid {
				e.CorrectCount = int(value.Int64)
			}
		case exam.FieldIsRunning:
			if value, ok := values[i].(*sql.NullBool); !ok {
				return fmt.Errorf("unexpected type %T for field is_running", values[i])
			} else if value.Valid {
				e.IsRunning = value.Bool
			}
		case exam.FieldTargetWordTypes:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field target_word_types", values[i])
			} else if value.Valid {
				e.TargetWordTypes = value.String
			}
		case exam.FieldChoicesPosIds:
			if value, ok := values[i].(*[]byte); !ok {
				return fmt.Errorf("unexpected type %T for field choices_pos_ids", values[i])
			} else if value != nil && len(*value) > 0 {
				if err := json.Unmarshal(*value, &e.ChoicesPosIds); err != nil {
					return fmt.Errorf("unmarshal field choices_pos_ids: %w", err)
				}
			}
		case exam.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				e.CreatedAt = value.Time
			}
		default:
			e.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the Exam.
// This includes values selected through modifiers, order, etc.
func (e *Exam) Value(name string) (ent.Value, error) {
	return e.selectValues.Get(name)
}

// QueryUser queries the "user" edge of the Exam entity.
func (e *Exam) QueryUser() *UserQuery {
	return NewExamClient(e.config).QueryUser(e)
}

// QueryExamQuestions queries the "exam_questions" edge of the Exam entity.
func (e *Exam) QueryExamQuestions() *ExamQuestionQuery {
	return NewExamClient(e.config).QueryExamQuestions(e)
}

// Update returns a builder for updating this Exam.
// Note that you need to call Exam.Unwrap() before calling this method if this Exam
// was returned from a transaction, and the transaction was committed or rolled back.
func (e *Exam) Update() *ExamUpdateOne {
	return NewExamClient(e.config).UpdateOne(e)
}

// Unwrap unwraps the Exam entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (e *Exam) Unwrap() *Exam {
	_tx, ok := e.config.driver.(*txDriver)
	if !ok {
		panic("ent: Exam is not a transactional entity")
	}
	e.config.driver = _tx.drv
	return e
}

// String implements the fmt.Stringer.
func (e *Exam) String() string {
	var builder strings.Builder
	builder.WriteString("Exam(")
	builder.WriteString(fmt.Sprintf("id=%v, ", e.ID))
	builder.WriteString("user_id=")
	builder.WriteString(fmt.Sprintf("%v", e.UserID))
	builder.WriteString(", ")
	builder.WriteString("total_questions=")
	builder.WriteString(fmt.Sprintf("%v", e.TotalQuestions))
	builder.WriteString(", ")
	builder.WriteString("correct_count=")
	builder.WriteString(fmt.Sprintf("%v", e.CorrectCount))
	builder.WriteString(", ")
	builder.WriteString("is_running=")
	builder.WriteString(fmt.Sprintf("%v", e.IsRunning))
	builder.WriteString(", ")
	builder.WriteString("target_word_types=")
	builder.WriteString(e.TargetWordTypes)
	builder.WriteString(", ")
	builder.WriteString("choices_pos_ids=")
	builder.WriteString(fmt.Sprintf("%v", e.ChoicesPosIds))
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(e.CreatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// Exams is a parsable slice of Exam.
type Exams []*Exam
