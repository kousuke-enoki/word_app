// Code generated by ent, DO NOT EDIT.

package ent

import (
	"encoding/json"
	"fmt"
	"strings"
	"time"
	"word_app/backend/ent/exam"
	"word_app/backend/ent/examquestion"
	"word_app/backend/ent/japanesemean"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
)

// ExamQuestion is the model entity for the ExamQuestion schema.
type ExamQuestion struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// ExamID holds the value of the "exam_id" field.
	ExamID int `json:"exam_id,omitempty"`
	// CorrectJpmID holds the value of the "correct_jpm_id" field.
	CorrectJpmID int `json:"correct_jpm_id,omitempty"`
	// ChoicesJpmIds holds the value of the "choices_jpm_ids" field.
	ChoicesJpmIds []int `json:"choices_jpm_ids,omitempty"`
	// Answer holds the value of the "answer" field.
	Answer *string `json:"answer,omitempty"`
	// IsCorrect holds the value of the "is_correct" field.
	IsCorrect bool `json:"is_correct,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the ExamQuestionQuery when eager-loading is set.
	Edges        ExamQuestionEdges `json:"edges"`
	selectValues sql.SelectValues
}

// ExamQuestionEdges holds the relations/edges for other nodes in the graph.
type ExamQuestionEdges struct {
	// Exam holds the value of the exam edge.
	Exam *Exam `json:"exam,omitempty"`
	// JapaneseMean holds the value of the japanese_mean edge.
	JapaneseMean *JapaneseMean `json:"japanese_mean,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [2]bool
}

// ExamOrErr returns the Exam value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e ExamQuestionEdges) ExamOrErr() (*Exam, error) {
	if e.Exam != nil {
		return e.Exam, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: exam.Label}
	}
	return nil, &NotLoadedError{edge: "exam"}
}

// JapaneseMeanOrErr returns the JapaneseMean value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e ExamQuestionEdges) JapaneseMeanOrErr() (*JapaneseMean, error) {
	if e.JapaneseMean != nil {
		return e.JapaneseMean, nil
	} else if e.loadedTypes[1] {
		return nil, &NotFoundError{label: japanesemean.Label}
	}
	return nil, &NotLoadedError{edge: "japanese_mean"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*ExamQuestion) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case examquestion.FieldChoicesJpmIds:
			values[i] = new([]byte)
		case examquestion.FieldIsCorrect:
			values[i] = new(sql.NullBool)
		case examquestion.FieldID, examquestion.FieldExamID, examquestion.FieldCorrectJpmID:
			values[i] = new(sql.NullInt64)
		case examquestion.FieldAnswer:
			values[i] = new(sql.NullString)
		case examquestion.FieldCreatedAt:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the ExamQuestion fields.
func (eq *ExamQuestion) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case examquestion.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			eq.ID = int(value.Int64)
		case examquestion.FieldExamID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field exam_id", values[i])
			} else if value.Valid {
				eq.ExamID = int(value.Int64)
			}
		case examquestion.FieldCorrectJpmID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field correct_jpm_id", values[i])
			} else if value.Valid {
				eq.CorrectJpmID = int(value.Int64)
			}
		case examquestion.FieldChoicesJpmIds:
			if value, ok := values[i].(*[]byte); !ok {
				return fmt.Errorf("unexpected type %T for field choices_jpm_ids", values[i])
			} else if value != nil && len(*value) > 0 {
				if err := json.Unmarshal(*value, &eq.ChoicesJpmIds); err != nil {
					return fmt.Errorf("unmarshal field choices_jpm_ids: %w", err)
				}
			}
		case examquestion.FieldAnswer:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field answer", values[i])
			} else if value.Valid {
				eq.Answer = new(string)
				*eq.Answer = value.String
			}
		case examquestion.FieldIsCorrect:
			if value, ok := values[i].(*sql.NullBool); !ok {
				return fmt.Errorf("unexpected type %T for field is_correct", values[i])
			} else if value.Valid {
				eq.IsCorrect = value.Bool
			}
		case examquestion.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				eq.CreatedAt = value.Time
			}
		default:
			eq.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the ExamQuestion.
// This includes values selected through modifiers, order, etc.
func (eq *ExamQuestion) Value(name string) (ent.Value, error) {
	return eq.selectValues.Get(name)
}

// QueryExam queries the "exam" edge of the ExamQuestion entity.
func (eq *ExamQuestion) QueryExam() *ExamQuery {
	return NewExamQuestionClient(eq.config).QueryExam(eq)
}

// QueryJapaneseMean queries the "japanese_mean" edge of the ExamQuestion entity.
func (eq *ExamQuestion) QueryJapaneseMean() *JapaneseMeanQuery {
	return NewExamQuestionClient(eq.config).QueryJapaneseMean(eq)
}

// Update returns a builder for updating this ExamQuestion.
// Note that you need to call ExamQuestion.Unwrap() before calling this method if this ExamQuestion
// was returned from a transaction, and the transaction was committed or rolled back.
func (eq *ExamQuestion) Update() *ExamQuestionUpdateOne {
	return NewExamQuestionClient(eq.config).UpdateOne(eq)
}

// Unwrap unwraps the ExamQuestion entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (eq *ExamQuestion) Unwrap() *ExamQuestion {
	_tx, ok := eq.config.driver.(*txDriver)
	if !ok {
		panic("ent: ExamQuestion is not a transactional entity")
	}
	eq.config.driver = _tx.drv
	return eq
}

// String implements the fmt.Stringer.
func (eq *ExamQuestion) String() string {
	var builder strings.Builder
	builder.WriteString("ExamQuestion(")
	builder.WriteString(fmt.Sprintf("id=%v, ", eq.ID))
	builder.WriteString("exam_id=")
	builder.WriteString(fmt.Sprintf("%v", eq.ExamID))
	builder.WriteString(", ")
	builder.WriteString("correct_jpm_id=")
	builder.WriteString(fmt.Sprintf("%v", eq.CorrectJpmID))
	builder.WriteString(", ")
	builder.WriteString("choices_jpm_ids=")
	builder.WriteString(fmt.Sprintf("%v", eq.ChoicesJpmIds))
	builder.WriteString(", ")
	if v := eq.Answer; v != nil {
		builder.WriteString("answer=")
		builder.WriteString(*v)
	}
	builder.WriteString(", ")
	builder.WriteString("is_correct=")
	builder.WriteString(fmt.Sprintf("%v", eq.IsCorrect))
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(eq.CreatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// ExamQuestions is a parsable slice of ExamQuestion.
type ExamQuestions []*ExamQuestion
