// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"
	"word_app/backend/ent/externalauth"
	"word_app/backend/ent/user"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
)

// ExternalAuth is the model entity for the ExternalAuth schema.
type ExternalAuth struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// Provider holds the value of the "provider" field.
	Provider string `json:"provider,omitempty"`
	// ProviderUserID holds the value of the "provider_user_id" field.
	ProviderUserID string `json:"provider_user_id,omitempty"`
	// DeletedAt holds the value of the "deleted_at" field.
	DeletedAt *time.Time `json:"deleted_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the ExternalAuthQuery when eager-loading is set.
	Edges               ExternalAuthEdges `json:"edges"`
	user_external_auths *int
	selectValues        sql.SelectValues
}

// ExternalAuthEdges holds the relations/edges for other nodes in the graph.
type ExternalAuthEdges struct {
	// User holds the value of the user edge.
	User *User `json:"user,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// UserOrErr returns the User value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e ExternalAuthEdges) UserOrErr() (*User, error) {
	if e.User != nil {
		return e.User, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: user.Label}
	}
	return nil, &NotLoadedError{edge: "user"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*ExternalAuth) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case externalauth.FieldID:
			values[i] = new(sql.NullInt64)
		case externalauth.FieldProvider, externalauth.FieldProviderUserID:
			values[i] = new(sql.NullString)
		case externalauth.FieldDeletedAt:
			values[i] = new(sql.NullTime)
		case externalauth.ForeignKeys[0]: // user_external_auths
			values[i] = new(sql.NullInt64)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the ExternalAuth fields.
func (ea *ExternalAuth) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case externalauth.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			ea.ID = int(value.Int64)
		case externalauth.FieldProvider:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field provider", values[i])
			} else if value.Valid {
				ea.Provider = value.String
			}
		case externalauth.FieldProviderUserID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field provider_user_id", values[i])
			} else if value.Valid {
				ea.ProviderUserID = value.String
			}
		case externalauth.FieldDeletedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field deleted_at", values[i])
			} else if value.Valid {
				ea.DeletedAt = new(time.Time)
				*ea.DeletedAt = value.Time
			}
		case externalauth.ForeignKeys[0]:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for edge-field user_external_auths", value)
			} else if value.Valid {
				ea.user_external_auths = new(int)
				*ea.user_external_auths = int(value.Int64)
			}
		default:
			ea.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the ExternalAuth.
// This includes values selected through modifiers, order, etc.
func (ea *ExternalAuth) Value(name string) (ent.Value, error) {
	return ea.selectValues.Get(name)
}

// QueryUser queries the "user" edge of the ExternalAuth entity.
func (ea *ExternalAuth) QueryUser() *UserQuery {
	return NewExternalAuthClient(ea.config).QueryUser(ea)
}

// Update returns a builder for updating this ExternalAuth.
// Note that you need to call ExternalAuth.Unwrap() before calling this method if this ExternalAuth
// was returned from a transaction, and the transaction was committed or rolled back.
func (ea *ExternalAuth) Update() *ExternalAuthUpdateOne {
	return NewExternalAuthClient(ea.config).UpdateOne(ea)
}

// Unwrap unwraps the ExternalAuth entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (ea *ExternalAuth) Unwrap() *ExternalAuth {
	_tx, ok := ea.config.driver.(*txDriver)
	if !ok {
		panic("ent: ExternalAuth is not a transactional entity")
	}
	ea.config.driver = _tx.drv
	return ea
}

// String implements the fmt.Stringer.
func (ea *ExternalAuth) String() string {
	var builder strings.Builder
	builder.WriteString("ExternalAuth(")
	builder.WriteString(fmt.Sprintf("id=%v, ", ea.ID))
	builder.WriteString("provider=")
	builder.WriteString(ea.Provider)
	builder.WriteString(", ")
	builder.WriteString("provider_user_id=")
	builder.WriteString(ea.ProviderUserID)
	builder.WriteString(", ")
	if v := ea.DeletedAt; v != nil {
		builder.WriteString("deleted_at=")
		builder.WriteString(v.Format(time.ANSIC))
	}
	builder.WriteByte(')')
	return builder.String()
}

// ExternalAuths is a parsable slice of ExternalAuth.
type ExternalAuths []*ExternalAuth
