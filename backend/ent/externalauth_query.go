// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"
	"word_app/backend/ent/externalauth"
	"word_app/backend/ent/predicate"
	"word_app/backend/ent/user"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// ExternalAuthQuery is the builder for querying ExternalAuth entities.
type ExternalAuthQuery struct {
	config
	ctx        *QueryContext
	order      []externalauth.OrderOption
	inters     []Interceptor
	predicates []predicate.ExternalAuth
	withUser   *UserQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the ExternalAuthQuery builder.
func (eaq *ExternalAuthQuery) Where(ps ...predicate.ExternalAuth) *ExternalAuthQuery {
	eaq.predicates = append(eaq.predicates, ps...)
	return eaq
}

// Limit the number of records to be returned by this query.
func (eaq *ExternalAuthQuery) Limit(limit int) *ExternalAuthQuery {
	eaq.ctx.Limit = &limit
	return eaq
}

// Offset to start from.
func (eaq *ExternalAuthQuery) Offset(offset int) *ExternalAuthQuery {
	eaq.ctx.Offset = &offset
	return eaq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (eaq *ExternalAuthQuery) Unique(unique bool) *ExternalAuthQuery {
	eaq.ctx.Unique = &unique
	return eaq
}

// Order specifies how the records should be ordered.
func (eaq *ExternalAuthQuery) Order(o ...externalauth.OrderOption) *ExternalAuthQuery {
	eaq.order = append(eaq.order, o...)
	return eaq
}

// QueryUser chains the current query on the "user" edge.
func (eaq *ExternalAuthQuery) QueryUser() *UserQuery {
	query := (&UserClient{config: eaq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := eaq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := eaq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(externalauth.Table, externalauth.FieldID, selector),
			sqlgraph.To(user.Table, user.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, externalauth.UserTable, externalauth.UserColumn),
		)
		fromU = sqlgraph.SetNeighbors(eaq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first ExternalAuth entity from the query.
// Returns a *NotFoundError when no ExternalAuth was found.
func (eaq *ExternalAuthQuery) First(ctx context.Context) (*ExternalAuth, error) {
	nodes, err := eaq.Limit(1).All(setContextOp(ctx, eaq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{externalauth.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (eaq *ExternalAuthQuery) FirstX(ctx context.Context) *ExternalAuth {
	node, err := eaq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first ExternalAuth ID from the query.
// Returns a *NotFoundError when no ExternalAuth ID was found.
func (eaq *ExternalAuthQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = eaq.Limit(1).IDs(setContextOp(ctx, eaq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{externalauth.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (eaq *ExternalAuthQuery) FirstIDX(ctx context.Context) int {
	id, err := eaq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single ExternalAuth entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one ExternalAuth entity is found.
// Returns a *NotFoundError when no ExternalAuth entities are found.
func (eaq *ExternalAuthQuery) Only(ctx context.Context) (*ExternalAuth, error) {
	nodes, err := eaq.Limit(2).All(setContextOp(ctx, eaq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{externalauth.Label}
	default:
		return nil, &NotSingularError{externalauth.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (eaq *ExternalAuthQuery) OnlyX(ctx context.Context) *ExternalAuth {
	node, err := eaq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only ExternalAuth ID in the query.
// Returns a *NotSingularError when more than one ExternalAuth ID is found.
// Returns a *NotFoundError when no entities are found.
func (eaq *ExternalAuthQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = eaq.Limit(2).IDs(setContextOp(ctx, eaq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{externalauth.Label}
	default:
		err = &NotSingularError{externalauth.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (eaq *ExternalAuthQuery) OnlyIDX(ctx context.Context) int {
	id, err := eaq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of ExternalAuths.
func (eaq *ExternalAuthQuery) All(ctx context.Context) ([]*ExternalAuth, error) {
	ctx = setContextOp(ctx, eaq.ctx, ent.OpQueryAll)
	if err := eaq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*ExternalAuth, *ExternalAuthQuery]()
	return withInterceptors[[]*ExternalAuth](ctx, eaq, qr, eaq.inters)
}

// AllX is like All, but panics if an error occurs.
func (eaq *ExternalAuthQuery) AllX(ctx context.Context) []*ExternalAuth {
	nodes, err := eaq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of ExternalAuth IDs.
func (eaq *ExternalAuthQuery) IDs(ctx context.Context) (ids []int, err error) {
	if eaq.ctx.Unique == nil && eaq.path != nil {
		eaq.Unique(true)
	}
	ctx = setContextOp(ctx, eaq.ctx, ent.OpQueryIDs)
	if err = eaq.Select(externalauth.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (eaq *ExternalAuthQuery) IDsX(ctx context.Context) []int {
	ids, err := eaq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (eaq *ExternalAuthQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, eaq.ctx, ent.OpQueryCount)
	if err := eaq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, eaq, querierCount[*ExternalAuthQuery](), eaq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (eaq *ExternalAuthQuery) CountX(ctx context.Context) int {
	count, err := eaq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (eaq *ExternalAuthQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, eaq.ctx, ent.OpQueryExist)
	switch _, err := eaq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (eaq *ExternalAuthQuery) ExistX(ctx context.Context) bool {
	exist, err := eaq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the ExternalAuthQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (eaq *ExternalAuthQuery) Clone() *ExternalAuthQuery {
	if eaq == nil {
		return nil
	}
	return &ExternalAuthQuery{
		config:     eaq.config,
		ctx:        eaq.ctx.Clone(),
		order:      append([]externalauth.OrderOption{}, eaq.order...),
		inters:     append([]Interceptor{}, eaq.inters...),
		predicates: append([]predicate.ExternalAuth{}, eaq.predicates...),
		withUser:   eaq.withUser.Clone(),
		// clone intermediate query.
		sql:  eaq.sql.Clone(),
		path: eaq.path,
	}
}

// WithUser tells the query-builder to eager-load the nodes that are connected to
// the "user" edge. The optional arguments are used to configure the query builder of the edge.
func (eaq *ExternalAuthQuery) WithUser(opts ...func(*UserQuery)) *ExternalAuthQuery {
	query := (&UserClient{config: eaq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	eaq.withUser = query
	return eaq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		UserID int `json:"user_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.ExternalAuth.Query().
//		GroupBy(externalauth.FieldUserID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (eaq *ExternalAuthQuery) GroupBy(field string, fields ...string) *ExternalAuthGroupBy {
	eaq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &ExternalAuthGroupBy{build: eaq}
	grbuild.flds = &eaq.ctx.Fields
	grbuild.label = externalauth.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		UserID int `json:"user_id,omitempty"`
//	}
//
//	client.ExternalAuth.Query().
//		Select(externalauth.FieldUserID).
//		Scan(ctx, &v)
func (eaq *ExternalAuthQuery) Select(fields ...string) *ExternalAuthSelect {
	eaq.ctx.Fields = append(eaq.ctx.Fields, fields...)
	sbuild := &ExternalAuthSelect{ExternalAuthQuery: eaq}
	sbuild.label = externalauth.Label
	sbuild.flds, sbuild.scan = &eaq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a ExternalAuthSelect configured with the given aggregations.
func (eaq *ExternalAuthQuery) Aggregate(fns ...AggregateFunc) *ExternalAuthSelect {
	return eaq.Select().Aggregate(fns...)
}

func (eaq *ExternalAuthQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range eaq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, eaq); err != nil {
				return err
			}
		}
	}
	for _, f := range eaq.ctx.Fields {
		if !externalauth.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if eaq.path != nil {
		prev, err := eaq.path(ctx)
		if err != nil {
			return err
		}
		eaq.sql = prev
	}
	return nil
}

func (eaq *ExternalAuthQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*ExternalAuth, error) {
	var (
		nodes       = []*ExternalAuth{}
		_spec       = eaq.querySpec()
		loadedTypes = [1]bool{
			eaq.withUser != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*ExternalAuth).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &ExternalAuth{config: eaq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, eaq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := eaq.withUser; query != nil {
		if err := eaq.loadUser(ctx, query, nodes, nil,
			func(n *ExternalAuth, e *User) { n.Edges.User = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (eaq *ExternalAuthQuery) loadUser(ctx context.Context, query *UserQuery, nodes []*ExternalAuth, init func(*ExternalAuth), assign func(*ExternalAuth, *User)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*ExternalAuth)
	for i := range nodes {
		fk := nodes[i].UserID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(user.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "user_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (eaq *ExternalAuthQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := eaq.querySpec()
	_spec.Node.Columns = eaq.ctx.Fields
	if len(eaq.ctx.Fields) > 0 {
		_spec.Unique = eaq.ctx.Unique != nil && *eaq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, eaq.driver, _spec)
}

func (eaq *ExternalAuthQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(externalauth.Table, externalauth.Columns, sqlgraph.NewFieldSpec(externalauth.FieldID, field.TypeInt))
	_spec.From = eaq.sql
	if unique := eaq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if eaq.path != nil {
		_spec.Unique = true
	}
	if fields := eaq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, externalauth.FieldID)
		for i := range fields {
			if fields[i] != externalauth.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if eaq.withUser != nil {
			_spec.Node.AddColumnOnce(externalauth.FieldUserID)
		}
	}
	if ps := eaq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := eaq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := eaq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := eaq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (eaq *ExternalAuthQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(eaq.driver.Dialect())
	t1 := builder.Table(externalauth.Table)
	columns := eaq.ctx.Fields
	if len(columns) == 0 {
		columns = externalauth.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if eaq.sql != nil {
		selector = eaq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if eaq.ctx.Unique != nil && *eaq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range eaq.predicates {
		p(selector)
	}
	for _, p := range eaq.order {
		p(selector)
	}
	if offset := eaq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := eaq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ExternalAuthGroupBy is the group-by builder for ExternalAuth entities.
type ExternalAuthGroupBy struct {
	selector
	build *ExternalAuthQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (eagb *ExternalAuthGroupBy) Aggregate(fns ...AggregateFunc) *ExternalAuthGroupBy {
	eagb.fns = append(eagb.fns, fns...)
	return eagb
}

// Scan applies the selector query and scans the result into the given value.
func (eagb *ExternalAuthGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, eagb.build.ctx, ent.OpQueryGroupBy)
	if err := eagb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ExternalAuthQuery, *ExternalAuthGroupBy](ctx, eagb.build, eagb, eagb.build.inters, v)
}

func (eagb *ExternalAuthGroupBy) sqlScan(ctx context.Context, root *ExternalAuthQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(eagb.fns))
	for _, fn := range eagb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*eagb.flds)+len(eagb.fns))
		for _, f := range *eagb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*eagb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := eagb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// ExternalAuthSelect is the builder for selecting fields of ExternalAuth entities.
type ExternalAuthSelect struct {
	*ExternalAuthQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (eas *ExternalAuthSelect) Aggregate(fns ...AggregateFunc) *ExternalAuthSelect {
	eas.fns = append(eas.fns, fns...)
	return eas
}

// Scan applies the selector query and scans the result into the given value.
func (eas *ExternalAuthSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, eas.ctx, ent.OpQuerySelect)
	if err := eas.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ExternalAuthQuery, *ExternalAuthSelect](ctx, eas.ExternalAuthQuery, eas, eas.inters, v)
}

func (eas *ExternalAuthSelect) sqlScan(ctx context.Context, root *ExternalAuthQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(eas.fns))
	for _, fn := range eas.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*eas.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := eas.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
