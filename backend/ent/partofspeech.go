// Code generated by ent, DO NOT EDIT.

package ent

import (
	"eng_app/ent/partofspeech"
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
)

// PartOfSpeech is the model entity for the PartOfSpeech schema.
type PartOfSpeech struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// Name holds the value of the "name" field.
	Name string `json:"name,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// UpdatedAt holds the value of the "updated_at" field.
	UpdatedAt time.Time `json:"updated_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the PartOfSpeechQuery when eager-loading is set.
	Edges        PartOfSpeechEdges `json:"edges"`
	selectValues sql.SelectValues
}

// PartOfSpeechEdges holds the relations/edges for other nodes in the graph.
type PartOfSpeechEdges struct {
	// WordInfos holds the value of the word_infos edge.
	WordInfos []*WordInfo `json:"word_infos,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// WordInfosOrErr returns the WordInfos value or an error if the edge
// was not loaded in eager-loading.
func (e PartOfSpeechEdges) WordInfosOrErr() ([]*WordInfo, error) {
	if e.loadedTypes[0] {
		return e.WordInfos, nil
	}
	return nil, &NotLoadedError{edge: "word_infos"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*PartOfSpeech) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case partofspeech.FieldID:
			values[i] = new(sql.NullInt64)
		case partofspeech.FieldName:
			values[i] = new(sql.NullString)
		case partofspeech.FieldCreatedAt, partofspeech.FieldUpdatedAt:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the PartOfSpeech fields.
func (pos *PartOfSpeech) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case partofspeech.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			pos.ID = int(value.Int64)
		case partofspeech.FieldName:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field name", values[i])
			} else if value.Valid {
				pos.Name = value.String
			}
		case partofspeech.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				pos.CreatedAt = value.Time
			}
		case partofspeech.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[i])
			} else if value.Valid {
				pos.UpdatedAt = value.Time
			}
		default:
			pos.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the PartOfSpeech.
// This includes values selected through modifiers, order, etc.
func (pos *PartOfSpeech) Value(name string) (ent.Value, error) {
	return pos.selectValues.Get(name)
}

// QueryWordInfos queries the "word_infos" edge of the PartOfSpeech entity.
func (pos *PartOfSpeech) QueryWordInfos() *WordInfoQuery {
	return NewPartOfSpeechClient(pos.config).QueryWordInfos(pos)
}

// Update returns a builder for updating this PartOfSpeech.
// Note that you need to call PartOfSpeech.Unwrap() before calling this method if this PartOfSpeech
// was returned from a transaction, and the transaction was committed or rolled back.
func (pos *PartOfSpeech) Update() *PartOfSpeechUpdateOne {
	return NewPartOfSpeechClient(pos.config).UpdateOne(pos)
}

// Unwrap unwraps the PartOfSpeech entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (pos *PartOfSpeech) Unwrap() *PartOfSpeech {
	_tx, ok := pos.config.driver.(*txDriver)
	if !ok {
		panic("ent: PartOfSpeech is not a transactional entity")
	}
	pos.config.driver = _tx.drv
	return pos
}

// String implements the fmt.Stringer.
func (pos *PartOfSpeech) String() string {
	var builder strings.Builder
	builder.WriteString("PartOfSpeech(")
	builder.WriteString(fmt.Sprintf("id=%v, ", pos.ID))
	builder.WriteString("name=")
	builder.WriteString(pos.Name)
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(pos.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("updated_at=")
	builder.WriteString(pos.UpdatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// PartOfSpeeches is a parsable slice of PartOfSpeech.
type PartOfSpeeches []*PartOfSpeech
