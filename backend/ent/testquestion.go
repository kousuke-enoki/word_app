// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"
	"word_app/backend/ent/registeredword"
	"word_app/backend/ent/test"
	"word_app/backend/ent/testquestion"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
)

// TestQuestion is the model entity for the TestQuestion schema.
type TestQuestion struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// TestID holds the value of the "test_id" field.
	TestID int `json:"test_id,omitempty"`
	// RegisteredWordID holds the value of the "registered_word_id" field.
	RegisteredWordID int `json:"registered_word_id,omitempty"`
	// IsCorrect holds the value of the "is_correct" field.
	IsCorrect bool `json:"is_correct,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the TestQuestionQuery when eager-loading is set.
	Edges        TestQuestionEdges `json:"edges"`
	selectValues sql.SelectValues
}

// TestQuestionEdges holds the relations/edges for other nodes in the graph.
type TestQuestionEdges struct {
	// Test holds the value of the test edge.
	Test *Test `json:"test,omitempty"`
	// RegisteredWord holds the value of the registered_word edge.
	RegisteredWord *RegisteredWord `json:"registered_word,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [2]bool
}

// TestOrErr returns the Test value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e TestQuestionEdges) TestOrErr() (*Test, error) {
	if e.Test != nil {
		return e.Test, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: test.Label}
	}
	return nil, &NotLoadedError{edge: "test"}
}

// RegisteredWordOrErr returns the RegisteredWord value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e TestQuestionEdges) RegisteredWordOrErr() (*RegisteredWord, error) {
	if e.RegisteredWord != nil {
		return e.RegisteredWord, nil
	} else if e.loadedTypes[1] {
		return nil, &NotFoundError{label: registeredword.Label}
	}
	return nil, &NotLoadedError{edge: "registered_word"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*TestQuestion) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case testquestion.FieldIsCorrect:
			values[i] = new(sql.NullBool)
		case testquestion.FieldID, testquestion.FieldTestID, testquestion.FieldRegisteredWordID:
			values[i] = new(sql.NullInt64)
		case testquestion.FieldCreatedAt:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the TestQuestion fields.
func (tq *TestQuestion) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case testquestion.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			tq.ID = int(value.Int64)
		case testquestion.FieldTestID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field test_id", values[i])
			} else if value.Valid {
				tq.TestID = int(value.Int64)
			}
		case testquestion.FieldRegisteredWordID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field registered_word_id", values[i])
			} else if value.Valid {
				tq.RegisteredWordID = int(value.Int64)
			}
		case testquestion.FieldIsCorrect:
			if value, ok := values[i].(*sql.NullBool); !ok {
				return fmt.Errorf("unexpected type %T for field is_correct", values[i])
			} else if value.Valid {
				tq.IsCorrect = value.Bool
			}
		case testquestion.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				tq.CreatedAt = value.Time
			}
		default:
			tq.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the TestQuestion.
// This includes values selected through modifiers, order, etc.
func (tq *TestQuestion) Value(name string) (ent.Value, error) {
	return tq.selectValues.Get(name)
}

// QueryTest queries the "test" edge of the TestQuestion entity.
func (tq *TestQuestion) QueryTest() *TestQuery {
	return NewTestQuestionClient(tq.config).QueryTest(tq)
}

// QueryRegisteredWord queries the "registered_word" edge of the TestQuestion entity.
func (tq *TestQuestion) QueryRegisteredWord() *RegisteredWordQuery {
	return NewTestQuestionClient(tq.config).QueryRegisteredWord(tq)
}

// Update returns a builder for updating this TestQuestion.
// Note that you need to call TestQuestion.Unwrap() before calling this method if this TestQuestion
// was returned from a transaction, and the transaction was committed or rolled back.
func (tq *TestQuestion) Update() *TestQuestionUpdateOne {
	return NewTestQuestionClient(tq.config).UpdateOne(tq)
}

// Unwrap unwraps the TestQuestion entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (tq *TestQuestion) Unwrap() *TestQuestion {
	_tx, ok := tq.config.driver.(*txDriver)
	if !ok {
		panic("ent: TestQuestion is not a transactional entity")
	}
	tq.config.driver = _tx.drv
	return tq
}

// String implements the fmt.Stringer.
func (tq *TestQuestion) String() string {
	var builder strings.Builder
	builder.WriteString("TestQuestion(")
	builder.WriteString(fmt.Sprintf("id=%v, ", tq.ID))
	builder.WriteString("test_id=")
	builder.WriteString(fmt.Sprintf("%v", tq.TestID))
	builder.WriteString(", ")
	builder.WriteString("registered_word_id=")
	builder.WriteString(fmt.Sprintf("%v", tq.RegisteredWordID))
	builder.WriteString(", ")
	builder.WriteString("is_correct=")
	builder.WriteString(fmt.Sprintf("%v", tq.IsCorrect))
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(tq.CreatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// TestQuestions is a parsable slice of TestQuestion.
type TestQuestions []*TestQuestion
