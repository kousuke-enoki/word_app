// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"
	"word_app/backend/ent/user"
	"word_app/backend/ent/userdailyusage"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
)

// UserDailyUsage is the model entity for the UserDailyUsage schema.
type UserDailyUsage struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// UserID holds the value of the "user_id" field.
	UserID int `json:"user_id,omitempty"`
	// JSTでの当日0時。これと今日JSTを比較してリセット判定する
	LastResetDate time.Time `json:"last_reset_date,omitempty"`
	// QuizCount holds the value of the "quiz_count" field.
	QuizCount int `json:"quiz_count,omitempty"`
	// BulkCount holds the value of the "bulk_count" field.
	BulkCount int `json:"bulk_count,omitempty"`
	// UpdatedAt holds the value of the "updated_at" field.
	UpdatedAt time.Time `json:"updated_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the UserDailyUsageQuery when eager-loading is set.
	Edges        UserDailyUsageEdges `json:"edges"`
	selectValues sql.SelectValues
}

// UserDailyUsageEdges holds the relations/edges for other nodes in the graph.
type UserDailyUsageEdges struct {
	// User holds the value of the user edge.
	User *User `json:"user,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// UserOrErr returns the User value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e UserDailyUsageEdges) UserOrErr() (*User, error) {
	if e.User != nil {
		return e.User, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: user.Label}
	}
	return nil, &NotLoadedError{edge: "user"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*UserDailyUsage) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case userdailyusage.FieldID, userdailyusage.FieldUserID, userdailyusage.FieldQuizCount, userdailyusage.FieldBulkCount:
			values[i] = new(sql.NullInt64)
		case userdailyusage.FieldLastResetDate, userdailyusage.FieldUpdatedAt:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the UserDailyUsage fields.
func (udu *UserDailyUsage) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case userdailyusage.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			udu.ID = int(value.Int64)
		case userdailyusage.FieldUserID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field user_id", values[i])
			} else if value.Valid {
				udu.UserID = int(value.Int64)
			}
		case userdailyusage.FieldLastResetDate:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field last_reset_date", values[i])
			} else if value.Valid {
				udu.LastResetDate = value.Time
			}
		case userdailyusage.FieldQuizCount:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field quiz_count", values[i])
			} else if value.Valid {
				udu.QuizCount = int(value.Int64)
			}
		case userdailyusage.FieldBulkCount:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field bulk_count", values[i])
			} else if value.Valid {
				udu.BulkCount = int(value.Int64)
			}
		case userdailyusage.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[i])
			} else if value.Valid {
				udu.UpdatedAt = value.Time
			}
		default:
			udu.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the UserDailyUsage.
// This includes values selected through modifiers, order, etc.
func (udu *UserDailyUsage) Value(name string) (ent.Value, error) {
	return udu.selectValues.Get(name)
}

// QueryUser queries the "user" edge of the UserDailyUsage entity.
func (udu *UserDailyUsage) QueryUser() *UserQuery {
	return NewUserDailyUsageClient(udu.config).QueryUser(udu)
}

// Update returns a builder for updating this UserDailyUsage.
// Note that you need to call UserDailyUsage.Unwrap() before calling this method if this UserDailyUsage
// was returned from a transaction, and the transaction was committed or rolled back.
func (udu *UserDailyUsage) Update() *UserDailyUsageUpdateOne {
	return NewUserDailyUsageClient(udu.config).UpdateOne(udu)
}

// Unwrap unwraps the UserDailyUsage entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (udu *UserDailyUsage) Unwrap() *UserDailyUsage {
	_tx, ok := udu.config.driver.(*txDriver)
	if !ok {
		panic("ent: UserDailyUsage is not a transactional entity")
	}
	udu.config.driver = _tx.drv
	return udu
}

// String implements the fmt.Stringer.
func (udu *UserDailyUsage) String() string {
	var builder strings.Builder
	builder.WriteString("UserDailyUsage(")
	builder.WriteString(fmt.Sprintf("id=%v, ", udu.ID))
	builder.WriteString("user_id=")
	builder.WriteString(fmt.Sprintf("%v", udu.UserID))
	builder.WriteString(", ")
	builder.WriteString("last_reset_date=")
	builder.WriteString(udu.LastResetDate.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("quiz_count=")
	builder.WriteString(fmt.Sprintf("%v", udu.QuizCount))
	builder.WriteString(", ")
	builder.WriteString("bulk_count=")
	builder.WriteString(fmt.Sprintf("%v", udu.BulkCount))
	builder.WriteString(", ")
	builder.WriteString("updated_at=")
	builder.WriteString(udu.UpdatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// UserDailyUsages is a parsable slice of UserDailyUsage.
type UserDailyUsages []*UserDailyUsage
