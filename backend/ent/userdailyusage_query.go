// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"
	"word_app/backend/ent/predicate"
	"word_app/backend/ent/user"
	"word_app/backend/ent/userdailyusage"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// UserDailyUsageQuery is the builder for querying UserDailyUsage entities.
type UserDailyUsageQuery struct {
	config
	ctx        *QueryContext
	order      []userdailyusage.OrderOption
	inters     []Interceptor
	predicates []predicate.UserDailyUsage
	withUser   *UserQuery
	withFKs    bool
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the UserDailyUsageQuery builder.
func (uduq *UserDailyUsageQuery) Where(ps ...predicate.UserDailyUsage) *UserDailyUsageQuery {
	uduq.predicates = append(uduq.predicates, ps...)
	return uduq
}

// Limit the number of records to be returned by this query.
func (uduq *UserDailyUsageQuery) Limit(limit int) *UserDailyUsageQuery {
	uduq.ctx.Limit = &limit
	return uduq
}

// Offset to start from.
func (uduq *UserDailyUsageQuery) Offset(offset int) *UserDailyUsageQuery {
	uduq.ctx.Offset = &offset
	return uduq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (uduq *UserDailyUsageQuery) Unique(unique bool) *UserDailyUsageQuery {
	uduq.ctx.Unique = &unique
	return uduq
}

// Order specifies how the records should be ordered.
func (uduq *UserDailyUsageQuery) Order(o ...userdailyusage.OrderOption) *UserDailyUsageQuery {
	uduq.order = append(uduq.order, o...)
	return uduq
}

// QueryUser chains the current query on the "user" edge.
func (uduq *UserDailyUsageQuery) QueryUser() *UserQuery {
	query := (&UserClient{config: uduq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := uduq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := uduq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(userdailyusage.Table, userdailyusage.FieldID, selector),
			sqlgraph.To(user.Table, user.FieldID),
			sqlgraph.Edge(sqlgraph.O2O, true, userdailyusage.UserTable, userdailyusage.UserColumn),
		)
		fromU = sqlgraph.SetNeighbors(uduq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first UserDailyUsage entity from the query.
// Returns a *NotFoundError when no UserDailyUsage was found.
func (uduq *UserDailyUsageQuery) First(ctx context.Context) (*UserDailyUsage, error) {
	nodes, err := uduq.Limit(1).All(setContextOp(ctx, uduq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{userdailyusage.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (uduq *UserDailyUsageQuery) FirstX(ctx context.Context) *UserDailyUsage {
	node, err := uduq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first UserDailyUsage ID from the query.
// Returns a *NotFoundError when no UserDailyUsage ID was found.
func (uduq *UserDailyUsageQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = uduq.Limit(1).IDs(setContextOp(ctx, uduq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{userdailyusage.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (uduq *UserDailyUsageQuery) FirstIDX(ctx context.Context) int {
	id, err := uduq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single UserDailyUsage entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one UserDailyUsage entity is found.
// Returns a *NotFoundError when no UserDailyUsage entities are found.
func (uduq *UserDailyUsageQuery) Only(ctx context.Context) (*UserDailyUsage, error) {
	nodes, err := uduq.Limit(2).All(setContextOp(ctx, uduq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{userdailyusage.Label}
	default:
		return nil, &NotSingularError{userdailyusage.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (uduq *UserDailyUsageQuery) OnlyX(ctx context.Context) *UserDailyUsage {
	node, err := uduq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only UserDailyUsage ID in the query.
// Returns a *NotSingularError when more than one UserDailyUsage ID is found.
// Returns a *NotFoundError when no entities are found.
func (uduq *UserDailyUsageQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = uduq.Limit(2).IDs(setContextOp(ctx, uduq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{userdailyusage.Label}
	default:
		err = &NotSingularError{userdailyusage.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (uduq *UserDailyUsageQuery) OnlyIDX(ctx context.Context) int {
	id, err := uduq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of UserDailyUsages.
func (uduq *UserDailyUsageQuery) All(ctx context.Context) ([]*UserDailyUsage, error) {
	ctx = setContextOp(ctx, uduq.ctx, ent.OpQueryAll)
	if err := uduq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*UserDailyUsage, *UserDailyUsageQuery]()
	return withInterceptors[[]*UserDailyUsage](ctx, uduq, qr, uduq.inters)
}

// AllX is like All, but panics if an error occurs.
func (uduq *UserDailyUsageQuery) AllX(ctx context.Context) []*UserDailyUsage {
	nodes, err := uduq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of UserDailyUsage IDs.
func (uduq *UserDailyUsageQuery) IDs(ctx context.Context) (ids []int, err error) {
	if uduq.ctx.Unique == nil && uduq.path != nil {
		uduq.Unique(true)
	}
	ctx = setContextOp(ctx, uduq.ctx, ent.OpQueryIDs)
	if err = uduq.Select(userdailyusage.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (uduq *UserDailyUsageQuery) IDsX(ctx context.Context) []int {
	ids, err := uduq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (uduq *UserDailyUsageQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, uduq.ctx, ent.OpQueryCount)
	if err := uduq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, uduq, querierCount[*UserDailyUsageQuery](), uduq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (uduq *UserDailyUsageQuery) CountX(ctx context.Context) int {
	count, err := uduq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (uduq *UserDailyUsageQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, uduq.ctx, ent.OpQueryExist)
	switch _, err := uduq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (uduq *UserDailyUsageQuery) ExistX(ctx context.Context) bool {
	exist, err := uduq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the UserDailyUsageQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (uduq *UserDailyUsageQuery) Clone() *UserDailyUsageQuery {
	if uduq == nil {
		return nil
	}
	return &UserDailyUsageQuery{
		config:     uduq.config,
		ctx:        uduq.ctx.Clone(),
		order:      append([]userdailyusage.OrderOption{}, uduq.order...),
		inters:     append([]Interceptor{}, uduq.inters...),
		predicates: append([]predicate.UserDailyUsage{}, uduq.predicates...),
		withUser:   uduq.withUser.Clone(),
		// clone intermediate query.
		sql:  uduq.sql.Clone(),
		path: uduq.path,
	}
}

// WithUser tells the query-builder to eager-load the nodes that are connected to
// the "user" edge. The optional arguments are used to configure the query builder of the edge.
func (uduq *UserDailyUsageQuery) WithUser(opts ...func(*UserQuery)) *UserDailyUsageQuery {
	query := (&UserClient{config: uduq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	uduq.withUser = query
	return uduq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		LastResetDate time.Time `json:"last_reset_date,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.UserDailyUsage.Query().
//		GroupBy(userdailyusage.FieldLastResetDate).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (uduq *UserDailyUsageQuery) GroupBy(field string, fields ...string) *UserDailyUsageGroupBy {
	uduq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &UserDailyUsageGroupBy{build: uduq}
	grbuild.flds = &uduq.ctx.Fields
	grbuild.label = userdailyusage.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		LastResetDate time.Time `json:"last_reset_date,omitempty"`
//	}
//
//	client.UserDailyUsage.Query().
//		Select(userdailyusage.FieldLastResetDate).
//		Scan(ctx, &v)
func (uduq *UserDailyUsageQuery) Select(fields ...string) *UserDailyUsageSelect {
	uduq.ctx.Fields = append(uduq.ctx.Fields, fields...)
	sbuild := &UserDailyUsageSelect{UserDailyUsageQuery: uduq}
	sbuild.label = userdailyusage.Label
	sbuild.flds, sbuild.scan = &uduq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a UserDailyUsageSelect configured with the given aggregations.
func (uduq *UserDailyUsageQuery) Aggregate(fns ...AggregateFunc) *UserDailyUsageSelect {
	return uduq.Select().Aggregate(fns...)
}

func (uduq *UserDailyUsageQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range uduq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, uduq); err != nil {
				return err
			}
		}
	}
	for _, f := range uduq.ctx.Fields {
		if !userdailyusage.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if uduq.path != nil {
		prev, err := uduq.path(ctx)
		if err != nil {
			return err
		}
		uduq.sql = prev
	}
	return nil
}

func (uduq *UserDailyUsageQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*UserDailyUsage, error) {
	var (
		nodes       = []*UserDailyUsage{}
		withFKs     = uduq.withFKs
		_spec       = uduq.querySpec()
		loadedTypes = [1]bool{
			uduq.withUser != nil,
		}
	)
	if uduq.withUser != nil {
		withFKs = true
	}
	if withFKs {
		_spec.Node.Columns = append(_spec.Node.Columns, userdailyusage.ForeignKeys...)
	}
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*UserDailyUsage).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &UserDailyUsage{config: uduq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, uduq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := uduq.withUser; query != nil {
		if err := uduq.loadUser(ctx, query, nodes, nil,
			func(n *UserDailyUsage, e *User) { n.Edges.User = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (uduq *UserDailyUsageQuery) loadUser(ctx context.Context, query *UserQuery, nodes []*UserDailyUsage, init func(*UserDailyUsage), assign func(*UserDailyUsage, *User)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*UserDailyUsage)
	for i := range nodes {
		if nodes[i].user_user_daily_usage == nil {
			continue
		}
		fk := *nodes[i].user_user_daily_usage
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(user.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "user_user_daily_usage" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (uduq *UserDailyUsageQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := uduq.querySpec()
	_spec.Node.Columns = uduq.ctx.Fields
	if len(uduq.ctx.Fields) > 0 {
		_spec.Unique = uduq.ctx.Unique != nil && *uduq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, uduq.driver, _spec)
}

func (uduq *UserDailyUsageQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(userdailyusage.Table, userdailyusage.Columns, sqlgraph.NewFieldSpec(userdailyusage.FieldID, field.TypeInt))
	_spec.From = uduq.sql
	if unique := uduq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if uduq.path != nil {
		_spec.Unique = true
	}
	if fields := uduq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, userdailyusage.FieldID)
		for i := range fields {
			if fields[i] != userdailyusage.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := uduq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := uduq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := uduq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := uduq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (uduq *UserDailyUsageQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(uduq.driver.Dialect())
	t1 := builder.Table(userdailyusage.Table)
	columns := uduq.ctx.Fields
	if len(columns) == 0 {
		columns = userdailyusage.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if uduq.sql != nil {
		selector = uduq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if uduq.ctx.Unique != nil && *uduq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range uduq.predicates {
		p(selector)
	}
	for _, p := range uduq.order {
		p(selector)
	}
	if offset := uduq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := uduq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// UserDailyUsageGroupBy is the group-by builder for UserDailyUsage entities.
type UserDailyUsageGroupBy struct {
	selector
	build *UserDailyUsageQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (udugb *UserDailyUsageGroupBy) Aggregate(fns ...AggregateFunc) *UserDailyUsageGroupBy {
	udugb.fns = append(udugb.fns, fns...)
	return udugb
}

// Scan applies the selector query and scans the result into the given value.
func (udugb *UserDailyUsageGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, udugb.build.ctx, ent.OpQueryGroupBy)
	if err := udugb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*UserDailyUsageQuery, *UserDailyUsageGroupBy](ctx, udugb.build, udugb, udugb.build.inters, v)
}

func (udugb *UserDailyUsageGroupBy) sqlScan(ctx context.Context, root *UserDailyUsageQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(udugb.fns))
	for _, fn := range udugb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*udugb.flds)+len(udugb.fns))
		for _, f := range *udugb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*udugb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := udugb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// UserDailyUsageSelect is the builder for selecting fields of UserDailyUsage entities.
type UserDailyUsageSelect struct {
	*UserDailyUsageQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (udus *UserDailyUsageSelect) Aggregate(fns ...AggregateFunc) *UserDailyUsageSelect {
	udus.fns = append(udus.fns, fns...)
	return udus
}

// Scan applies the selector query and scans the result into the given value.
func (udus *UserDailyUsageSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, udus.ctx, ent.OpQuerySelect)
	if err := udus.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*UserDailyUsageQuery, *UserDailyUsageSelect](ctx, udus.UserDailyUsageQuery, udus, udus.inters, v)
}

func (udus *UserDailyUsageSelect) sqlScan(ctx context.Context, root *UserDailyUsageQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(udus.fns))
	for _, fn := range udus.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*udus.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := udus.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
